apiVersion: v1
kind: Service
metadata:
  name: hello-javalin
  namespace: default
  labels:
    app: hello-javalin
spec:
  type: ClusterIP
  selector:
    app: hello-javalin
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 7100
